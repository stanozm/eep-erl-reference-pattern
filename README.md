eep-erl-reference-pattern
=========================

Test can be executed as:

```
distributed_attack_test:start(PathToLogFile).
```

The emitted results from both patterns will printed to standard output.
